#include "task1.h"
#include <fstream>
using namespace std;


int main() {

    int arr[LARGE_SIZE];
    int arr_copy[LARGE_SIZE];

    int size = 0;

    ofstream fout;
    fout.open(R"(D:\C++\Ex03\result1.txt)");
    
    for (int i = 0; i < 100; i++) {  

        size += DELTA_SIZE;

        // ???????????? ?? ???????
        srand(time(nullptr));

        // ?????????? ????????? ??????
        generate(arr, arr + size, rnd());
        copy(arr, arr + size, arr_copy);

        fout << size << "\t";

        // ???????? ?????
        time_t start = clock();

        // ????????? ??????????, ????????? ??????? qsort
        qsort(arr, size, sizeof(int), comp);

        fout << static_cast<double>(clock() - start) / CLOCKS_PER_SEC << "\t";

        // ????? ???????? ?????
        start = clock();

        // ????????? ??????????, ????????? ???????? ?++
        sort(arr_copy, arr_copy + size);

        fout << static_cast<double>(clock() - start) / CLOCKS_PER_SEC << "\n";

    }

    fout.close();
    return 0;
}